function viewBusiness(var id){
	// so ideally ID would be the actual ID of the business,
	// assuming thats what the Yelp API wants. Not sure how
	// to go about getting this, probably have to modify getPics
	// to also return ID, since it only returns name and url now
}